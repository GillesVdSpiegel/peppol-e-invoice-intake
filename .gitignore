# Fetched validation artefacts: pinned by artefacts/MANIFEST.json, not redistributed.
artefacts/*
!artefacts/MANIFEST.json
!artefacts/NOTICE.md

# The synthetic corpus is reproducible from code, so it is generated rather than
# committed. Phase 5's real supplier invoices must never enter version control
# at all - they are other people's commercial data.
/corpus/

.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
