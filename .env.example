# Copy to .env and put your Anthropic API key after the equals sign.
# .env is gitignored; only the convert and eval commands read it, and only
# ANTHROPIC_API_KEY is taken from it.
ANTHROPIC_API_KEY=
